pipeline {
  agent any

   tools {nodejs "nodejs22"}

     stages {
         stage('Dependencies') {
               steps {
                bat 'npm i'
                   }
            }
      stage('e2e Tests') {
            steps {
                   bat 'cypress run'
                  }
             }
      }
}
