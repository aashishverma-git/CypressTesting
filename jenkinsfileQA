pipeline {
  agent any

   tools {nodejs "nodejs22"}

     stages {
         stage('Dependencies') {
            steps {
                npm.cmd 'i'
                  }
}
      stage('e2e Tests') {
            steps {
                   npm.cmd 'cypress run'
                  }
             }
      }
}
