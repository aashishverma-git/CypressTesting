pipeline {
  agent any

   tools {nodejs "nodejs22"}

     stages {
         stage('Dependencies') {
               steps {
                cmd 'npm i'
                   }
            }
      stage('e2e Tests') {
            steps {
                   cmd 'cypress run'
                  }
             }
      }
}
