pipeline {
  agent any

   tools {nodejs "nodejs22"}

     stages {
         stage('Dependencies') {
            steps {
                npm 'i'
                  }
}
      stage('e2e Tests') {
            steps {
                   npx 'cypress run'
                  }
             }
      }
}
