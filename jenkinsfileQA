pipeline {
  agent any

   tools {nodejs "nodejs22"}

     stages {
         stage('Dependencies') {
            steps {
                npm 'i'
                npm 'fund'
                  }
}
      stage('e2e Tests') {
            steps {
                   npm 'cypress run'
                  }
             }
      }
}
