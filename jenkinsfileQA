pipeline {
  agent any

   tools {nodejs "nodejs22.9.0"}

  stages {
         stage('Dependencies') {
            steps {
                npm 'i'
                  }
}
      stage('e2e Tests') {
            steps {
                   npm 'cypress run'
                  }
             }
      }
}
